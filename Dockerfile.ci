FROM anatolelucet/neovim:0.12-bookworm

RUN apt-get update -qq \
 && apt-get install -y -qq --no-install-recommends \
     build-essential \
     curl \
     git \
     luarocks \
 && luarocks install luacheck \
 && rm -rf /var/lib/apt/lists/*
